# Copy this file to .env and fill in your values.
# .env is gitignored — never commit it.

# Project path on your host machine.
# If unset, Docker Compose falls back to the current directory (.).
WORKSPACE_PATH=C:\path\to\your\esp8266-traffic-light
